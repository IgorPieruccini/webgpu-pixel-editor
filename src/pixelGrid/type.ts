/**
 * A Grid representing the pixels that can be draw into the canvas
 * the key is a "floating number" as the integer as xAxis and the decimals as YAxis
 */
export type PixelGrid = Map<number, string>;
